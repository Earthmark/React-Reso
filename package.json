{
  "packageManager": "pnpm@10.22.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "turbo": "^2.6.1",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  }
}
